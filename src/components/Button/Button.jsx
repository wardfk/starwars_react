import ButtonContainer from "./Buton-style";

const pageButton = ({children}) => {
    return(
        <ButtonContainer>
            {children}
        </ButtonContainer>
    );

};

export default pageButton;