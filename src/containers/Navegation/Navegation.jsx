import NavegationContainer from "./Navegation-style";

const Navegation = ({children}) => {
    return(
        <NavegationContainer>
            {children}
        </NavegationContainer>
    );  
};

export default Navegation;